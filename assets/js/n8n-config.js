// n8n Webhook endpoints (no secrets committed). Fill these in production.
// - SECTIONING_WEBHOOK: 선택과목 분반 요청용 Webhook URL
// - TIMETABLE_WEBHOOK:  시간표 최적화 요청용 Webhook URL
// - AUTH_TOKEN:         선택(있으면 Authorization 헤더에 Bearer로 첨부)

window.N8N_SECTIONING_WEBHOOK = '';
window.N8N_TIMETABLE_WEBHOOK = '';
window.N8N_AUTH_TOKEN = '';

