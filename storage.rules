rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Block direct access; all uploads/downloads must use signed URLs via server
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

